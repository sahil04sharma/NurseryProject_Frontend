import React from "react";

const ProcessingLoader = () => {
  return (
    <span className="inset-0 flex items-center justify-center">
      <span className="loader-big" style={{ width: 20, height: 20 }} />
    </span>
  );
};

export default ProcessingLoader;
